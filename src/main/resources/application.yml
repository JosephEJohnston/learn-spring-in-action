#spring.thymeleaf.cache=false
spring:
  cassandra:
    keyspace-name: taco_cloud # 使用的键空间
    schema-action: recreate # 应用启动时废弃并重建所有表
    local-datacenter: datacenter1 # 确定本地数据中心的名称
    username: tacocloud
    password: s3cr3tP455w0rd